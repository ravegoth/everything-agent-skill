name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repository:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate repository structure, manifests, and privacy rules
        run: python tests/validate_repo.py
      - name: Parse scripts with PowerShell 7
        shell: pwsh
        run: |
          Get-ChildItem . -Filter '*.ps1' -Recurse | ForEach-Object {
            [void][scriptblock]::Create((Get-Content $_.FullName -Raw))
          }
      # Run out of process with -File, which is how the skill documents invoking
      # the script and the only form that preserves the script's exit code.
      - name: Detection reports non-Windows honestly
        shell: pwsh
        run: |
          $script = (Resolve-Path ./skills/everything-search/scripts/detect_everything.ps1).Path
          $outFile = Join-Path ([IO.Path]::GetTempPath()) 'detect-non-windows.json'
          $process = Start-Process -FilePath 'pwsh' -Wait -PassThru -NoNewWindow `
            -ArgumentList @('-NoProfile', '-File', $script) -RedirectStandardOutput $outFile
          if ($process.ExitCode -ne 2) { throw "Expected exit code 2 off Windows, got $($process.ExitCode)." }
          $status = (Get-Content -LiteralPath $outFile -Raw) | ConvertFrom-Json
          if ($status.platform.windows) { throw 'Expected platform.windows to be false.' }
          if ($status.backend.ready) { throw 'Expected backend.ready to be false.' }
          if ($status.everything.ipc) { throw 'Expected everything.ipc to be false.' }
          exit 0

  powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Parse scripts with Windows PowerShell 5.1
        shell: powershell
        run: |
          Get-ChildItem . -Filter '*.ps1' -Recurse | ForEach-Object {
            [void][scriptblock]::Create((Get-Content $_.FullName -Raw))
          }

      - name: Parse scripts with PowerShell 7
        shell: pwsh
        run: |
          Get-ChildItem . -Filter '*.ps1' -Recurse | ForEach-Object {
            [void][scriptblock]::Create((Get-Content $_.FullName -Raw))
          }

      # Everything is not installed on hosted runners, so these assert the
      # contract that must hold when no backend is present.
      - name: Test detection contract
        shell: powershell
        run: |
          $json = & ./skills/everything-search/scripts/detect_everything.ps1
          $status = $json | ConvertFrom-Json
          if (-not $status.platform.windows) { throw 'Expected Windows detection result.' }
          if ($null -eq $status.everything.installed) { throw 'Missing installed flag.' }
          if ($null -eq $status.backend.ready) { throw 'Missing backend ready flag.' }
          if ($status.everything.installed -isnot [bool]) { throw 'installed must be a boolean.' }
          if ($status.everything.ipc -isnot [bool]) { throw 'everything.ipc must be a boolean.' }
          if ($status.backend.ready -isnot [bool]) { throw 'backend.ready must be a boolean.' }
          exit 0

      # Regression: absent paths must serialize as JSON null, never as {}.
      # Select-Object -First 1 over an empty pipeline yields AutomationNull,
      # which ConvertTo-Json renders as an empty object.
      - name: Absent paths serialize as null
        shell: powershell
        run: |
          $status = & ./skills/everything-search/scripts/detect_everything.ps1 | ConvertFrom-Json
          foreach ($name in 'dll', 'es') {
            $value = $status.backend.$name
            if ($null -ne $value -and $value -isnot [string]) {
              throw "backend.$name must be null or a string path, got $($value.GetType().Name)."
            }
          }
          $exe = $status.everything.executable
          if ($null -ne $exe -and $exe -isnot [string]) {
            throw "everything.executable must be null or a string path, got $($exe.GetType().Name)."
          }
          exit 0

      # Invoked out of process because the script reports failures on the real
      # stderr handle, which an in-process redirect does not capture.
      - name: Search fails honestly with no backend
        shell: powershell
        run: |
          $script = (Resolve-Path ./skills/everything-search/scripts/search_everything.ps1).Path
          $outFile = Join-Path $env:RUNNER_TEMP 'search-stdout.txt'
          $errorFile = Join-Path $env:RUNNER_TEMP 'search-stderr.txt'
          $process = Start-Process -FilePath 'powershell.exe' -Wait -PassThru -NoNewWindow `
            -ArgumentList @('-NoProfile', '-ExecutionPolicy', 'Bypass', '-File', "`"$script`"", '-Query', 'ext:ps1') `
            -RedirectStandardOutput $outFile -RedirectStandardError $errorFile
          if ($process.ExitCode -eq 0) { throw 'Expected a nonzero exit code without a backend.' }
          if ((Get-Content -LiteralPath $outFile -Raw)) { throw 'No partial JSON may be written to stdout on failure.' }
          if (-not (Get-Content -LiteralPath $errorFile -Raw)) { throw 'Expected an explanatory message on stderr.' }

      # Regression: es.exe reads one space-containing argument as a single
      # literal phrase, so query terms must be split while quotes are kept.
      - name: Query tokenizer splits terms and preserves quoted phrases
        shell: powershell
        run: |
          $path = (Resolve-Path ./skills/everything-search/scripts/search_everything.ps1).Path
          $ast = [System.Management.Automation.Language.Parser]::ParseFile($path, [ref]$null, [ref]$null)
          $function = $ast.FindAll({
            $args[0] -is [System.Management.Automation.Language.FunctionDefinitionAst] -and
            $args[0].Name -eq 'Split-EsQueryTerm'
          }, $true)
          if (-not $function) { throw 'Split-EsQueryTerm was not found.' }
          . ([scriptblock]::Create($function[0].Extent.Text))

          $cases = @(
            @{ Query = 'ext:ps1 dm:today';                Expected = @('ext:ps1', 'dm:today') }
            @{ Query = 'path:"C:\Program Files" ext:txt'; Expected = @('path:"C:\Program Files"', 'ext:txt') }
            @{ Query = '"exact phrase"';                  Expected = @('"exact phrase"') }
            @{ Query = '  spaced   out  ';                Expected = @('spaced', 'out') }
            @{ Query = 'single';                          Expected = @('single') }
          )
          foreach ($case in $cases) {
            $actual = @(Split-EsQueryTerm -Text $case.Query)
            if ($actual.Count -ne $case.Expected.Count -or (Compare-Object $actual $case.Expected -SyncWindow 0)) {
              throw "Tokenizing '$($case.Query)' produced [$($actual -join '|')]."
            }
          }

      # Regression: cloud placeholder files carry attribute bits that the
      # running .NET may not define, and casting the raw mask to
      # [IO.FileAttributes] throws and aborts the whole search.
      - name: Attribute decoding survives unknown bits
        shell: powershell
        run: |
          $path = (Resolve-Path ./skills/everything-search/scripts/search_everything.ps1).Path
          $ast = [System.Management.Automation.Language.Parser]::ParseFile($path, [ref]$null, [ref]$null)
          $function = $ast.FindAll({
            $args[0] -is [System.Management.Automation.Language.FunctionDefinitionAst] -and
            $args[0].Name -eq 'Convert-FileAttribute'
          }, $true)
          if (-not $function) { throw 'Convert-FileAttribute was not found.' }
          . ([scriptblock]::Create($function[0].Extent.Text))

          # 0x400021 is the OneDrive placeholder bit plus ReadOnly and Archive.
          $cloud = Convert-FileAttribute -Value 4194337
          if ($cloud -notmatch 'ReadOnly' -or $cloud -notmatch 'Archive' -or $cloud -notmatch '0x400000') {
            throw "Unknown attribute bits were not preserved: $cloud"
          }
          if ((Convert-FileAttribute -Value 32) -ne 'Archive') { throw 'Archive did not decode.' }
          if ($null -ne (Convert-FileAttribute -Value 0)) { throw 'Zero must decode to null.' }
          if ($null -ne (Convert-FileAttribute -Value ([UInt32]::MaxValue))) {
            throw 'INVALID_FILE_ATTRIBUTES must decode to null.'
          }
          exit 0

      # Regression: with a legacy console code page, non-ASCII characters in
      # paths are transcoded on the way out and some become raw control bytes
      # (U+2022 is 0x07 in CP437), producing JSON that cannot be parsed.
      - name: JSON output is UTF-8 under a legacy console code page
        shell: powershell
        run: |
          foreach ($name in 'detect_everything', 'search_everything') {
            $text = Get-Content -LiteralPath "./skills/everything-search/scripts/$name.ps1" -Raw
            if ($text -notmatch '\[Console\]::OutputEncoding\s*=\s*New-Object\s+System\.Text\.UTF8Encoding') {
              throw "$name.ps1 must force UTF-8 output encoding."
            }
          }

          $outFile = Join-Path $env:RUNNER_TEMP 'encoding-probe.json'
          $probe = Join-Path $env:RUNNER_TEMP 'probe.ps1'
          $probeLines = @(
            'try { [Console]::OutputEncoding = New-Object System.Text.UTF8Encoding $false } catch { }'
            "[ordered]@{ name = ('a' + [char]0x2022 + 'b') } | ConvertTo-Json -Compress"
          )
          Set-Content -LiteralPath $probe -Value $probeLines -Encoding UTF8

          cmd /c "chcp 437 >nul && powershell.exe -NoProfile -ExecutionPolicy Bypass -File ""$probe"" > ""$outFile"""

          $bytes = [IO.File]::ReadAllBytes($outFile)
          $control = @($bytes | Where-Object { $_ -lt 0x20 -and $_ -notin 0x09, 0x0A, 0x0D })
          if ($control.Count -gt 0) { throw "Raw control bytes in JSON output: $($control -join ',')" }
          $decoded = [Text.Encoding]::UTF8.GetString($bytes).Trim()
          if ((ConvertFrom-Json $decoded).name -ne ('a' + [char]0x2022 + 'b')) {
            throw "U+2022 did not survive the round trip: $decoded"
          }
          exit 0

      - name: Test setup dry run
        shell: powershell
        run: |
          ./skills/everything-search/scripts/setup_backend.ps1 -Component Sdk -WhatIf
          ./skills/everything-search/scripts/setup_backend.ps1 -Component Es -WhatIf
          exit 0

      # Regression: -WhatIf must stay a preview even when the backend is already
      # installed, which never happens on a fresh runner by default.
      - name: Setup dry run is safe when the backend already exists
        shell: powershell
        run: |
          $target = Join-Path $env:RUNNER_TEMP 'EverythingAgentExisting'
          New-Item -ItemType Directory -Path (Join-Path $target 'SDK') -Force | Out-Null
          New-Item -ItemType Directory -Path (Join-Path $target 'CLI') -Force | Out-Null
          ./skills/everything-search/scripts/setup_backend.ps1 -Component Sdk -Destination $target -WhatIf
          ./skills/everything-search/scripts/setup_backend.ps1 -Component Es -Destination $target -WhatIf
          $downloaded = @(Get-ChildItem -LiteralPath $target -Recurse -File)
          if ($downloaded.Count -ne 0) { throw 'WhatIf must not download or write any file.' }
          exit 0

      - name: Scripts run from a path containing spaces
        shell: powershell
        run: |
          $target = Join-Path $env:RUNNER_TEMP 'path with spaces\everything skill'
          New-Item -ItemType Directory -Path $target -Force | Out-Null
          Copy-Item -Path './skills' -Destination $target -Recurse -Force
          $script = Join-Path $target 'skills\everything-search\scripts\detect_everything.ps1'
          $status = & $script | ConvertFrom-Json
          if (-not $status.platform.windows) { throw 'Detection failed from a path containing spaces.' }
          exit 0

  skills-cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Skill is discoverable by the Skills CLI
        run: npx --yes skills@latest add . --list
